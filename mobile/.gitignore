# Expo / React Native — adds to the root .gitignore
.expo/
dist/
web-build/
expo-env.d.ts

# Native build folders (generated by `expo prebuild` / EAS)
/ios/
/android/

# Metro
.metro-health-check*

# Signing / keystores — never commit
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# macOS
.DS_Store
